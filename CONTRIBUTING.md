# 일하는 방식 — 한 장 요약

> 이 저장소(`.github`)의 양식은 **자기 양식이 없는 모든 저장소에 자동 적용**됩니다.
> 저장소에 같은 이름의 파일을 두면 그쪽이 우선합니다(덧붙이기가 아니라 바꿔치기 — 그래서 여기 것을 복사해 고치세요).
> 전체 규칙의 정본은 리더 세션이 들고 있는 전역 규칙 §7입니다. 여기엔 매일 쓰는 것만 적습니다.

## 1. 다섯 가지 원칙 (예외 없음)

1. **이슈 없이 코드를 건드리지 않는다.** 착수 전에 `#번호`를 먼저 말한다.
2. **모든 변경은 PR을 거친다.** 어떤 브랜치에도 직접 push하지 않는다.
3. **만든 사람이 머지하지 않는다.** 코더의 PR은 리더가, 리더의 릴리스는 사용자가 승인한다.
4. **되돌리기 어려운 일**(머지·배포·삭제·외부 발송·운영 DB)은 실행 전에 승인받는다.
5. **규칙을 못 지킬 상황이면 어기지 말고 예외를 기록한다.** 이유를 PR이나 이슈에 남긴다.

## 2. 브랜치 — 다섯 줄

| 브랜치 | 쉽게 말하면 | 누가 만드나 | 어디로 합치나 |
|---|---|---|---|
| `main` (또는 `master`) | **지금 손님이 쓰고 있는 것** | — (영구) | — |
| `develop` | **다음 출시에 나갈 것을 모으는 곳** · 기본 브랜치 | — (영구) | — |
| `feature/<이슈#>-<영문-이름>` | 이슈 하나를 만드는 작업대 | 코더 | develop (squash) |
| `release/vX.Y.Z` | 출시 전 얼려서 다듬는 곳 · **새 기능 금지** | 리더 | main **그리고** develop |
| `hotfix/<이슈#>-<영문-이름>` | 운영이 지금 망가졌을 때 질러가는 길 | 리더 | main **그리고** develop |

- 브랜치 이름은 **영문 소문자·숫자·하이픈만.** 한글 금지.
- 어느 브랜치로? 운영이 지금 망가졌고 못 기다린다 → `hotfix` / release 테스트 중 발견한 버그 → 그 `release` / **그 외 전부 → `feature`**. 애매하면 feature.

## 3. 이슈 — 양식 4종

| 양식 | 언제 |
|---|---|
| 🐛 버그 | 되던 것이 안 된다 |
| ✨ 기능 | 새로 만들거나 바꾸고 싶다 — **"무엇이 되면 끝인가"(수용 기준)가 핵심** |
| 📝 문서 | 코드는 그대로, 문서만 |
| 🤔 결정 요청 | 선택지가 갈려 사용자가 정해야 한다 |

우선순위: **P0** 운영 멈춤(즉시, hotfix) · **P1** 핵심 기능 불가(이번 출시 필수) · **P2** 우회 가능(순서대로) · **P3** 개선 → **등록하지 않고** 누적 이슈에 코멘트 한 줄.

라벨은 세 축만: `type:` · `P0~P3` · `status:`(blocked / in-progress / needs-info).

## 4. 커밋과 PR

- 커밋 제목: `feat(영역): 무엇을` / `fix(영역): 무엇을` / `docs: …` / `chore: …` — 명령형, 50자 안.
- 커밋 본문 끝에 **`Closes #번호`** — 제목의 `(#번호)`는 이슈를 닫지 않습니다.
- PR 본문 다섯 칸: 무엇을 / 왜 / 검증 / `Closes #N` / 티어(리더가 적음).
- PR 하나 = 이슈 하나 = 400줄 이하. 검증(타입·린트·빌드) 통과 전엔 **Draft**.
- 리뷰 코멘트 머리말: `blocking:` 고쳐야 머지 · `question:` 답 필요 · `suggestion:` / `nit:` / `praise:` 안 막음.

## 5. 머지 게이트 — 세 조건이 다 차야 합친다

① 코드리뷰 등록 ② 티어별 검증 사인오프 ③ CI 전 항목 통과. **CI 초록불은 승인이 아닙니다.**

| 티어 | 어떤 변경 | 검증 |
|---|---|---|
| A | 되돌리기 어렵거나 **조용히 틀릴 수 있는** 것 — 운영 브랜치 대상 PR·인증·결제·DB 스키마·공유 인프라·애매한 것 | 리뷰어 + QA 독립 검증 |
| B | 눈으로 재현되는 동작 변경 | 리뷰어 + 코더의 재현 증거 |
| C | 조용히 틀릴 수 없는 것 | 코더 자가 검증 + 리더 확인 |

## 6. 출시와 배포

사용자 "이번 것으로 출시" → 리더가 `release/vX.Y.Z` 생성 → 릴리스 QA → 버그만 수정 → `main` 머지 + 태그 → **develop으로 역병합** → 리더 "배포 준비 완료" 보고 → **사용자 최종 승인** → 배포 → 확인 보고.
`main`에 들어간 것과 배포된 것은 다릅니다. 문제가 생기면 고치기 전에 **먼저 되돌립니다.**

## 7. 누가 무엇을 하나

| 역할 | 하는 일 | 브랜치에 쓰나 |
|---|---|---|
| 사용자 | 출시 지시 · 배포 최종 승인 · hotfix 착수 승인 · 결정 | — |
| 리더 `/abiman-leader` | 범위·티어 결정, 역할 배정, **머지·릴리스 운영** | release·hotfix 생성, 머지만 |
| 코더 `/abiman-coder` | 구현 → 검증 → PR | feature |
| 문서담당 `/abiman-docs` | 가이드·릴리스 노트 | docs 브랜치 |
| 플래너 `/abiman-planner` | 계획서 (코드 읽기 전용) | ✗ |
| 조사관·리서치·검토자·이슈어·리뷰어·QA | 리더가 부르는 위임 역할 — 읽고 확인하고 말한다 | ✗ |

**외우기 쉬운 세 문장** — main과 develop에 직접 push하는 사람은 없다 · 자기 PR을 자기가 머지하는 사람은 없다 · main 머지는 배포가 아니다.
